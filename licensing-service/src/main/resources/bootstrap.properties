spring.application.name=licensing-service
server.port=8080
spring.config.import=optional:configserver:${CONFIGSERVER_URI:http://localhost:8071}
spring.profiles.active=${PROFILE:dev}
encrypt.key=${ENCRYPT_KEY}